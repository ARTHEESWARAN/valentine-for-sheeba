
Valentine Page – For Sheeba 💝
==================================

How to use
----------
1) Share as a link:
   • Upload `index.html` anywhere (GitHub Pages, Netlify Drop, Vercel) and share the URL.

Quick deploy options
--------------------
• GitHub Pages:
  - Create a repo (e.g., valentine-for-sheeba)
  - Add `index.html` in the root and commit
  - Settings → Pages → Source: Deploy from a branch; Branch: main (root)
  - Wait ~1 minute. Your site appears at https://<username>.github.io/valentine-for-sheeba/

• Netlify Drop:
  - Go to https://app.netlify.com/drop
  - Drag-and-drop the folder containing `index.html`
  - Copy the URL Netlify gives you and share

• Vercel:
  - Create a new project, import the repo (or upload)
  - Click Deploy, then share the https://…vercel.app link

Customize
---------
- To change the questions, open `index.html` and edit the `questions` array in the <script> section.
- To change the GIF, replace the src on the <img class="gif" ...> element with any public GIF URL.
- Colors can be adjusted in the CSS variables at the top of the <style> block.

Notes
-----
- This page is fully client-side and works on desktop and mobile.
- The “No” button only runs away on the final question.
- Link previews (WhatsApp, etc.) use the Open Graph tags set in <head>.

Made with ❤️  
